# CoRE Router Demo - Python Dependencies

# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Plotting and visualization (required by parent modules)
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# LLM client
openai>=1.0.0

# vLLM for embedding generation (PRIMARY - for Qwen3-0.6B)
vllm>=0.6.0

# Embedding model (FALLBACK - if not using vLLM)
sentence-transformers>=2.2.0
torch>=2.0.0

# Web interface
gradio>=4.0.0

# Optional: GPU acceleration
# Automatically handled by vLLM and PyTorch if CUDA is available
